import React from 'react';
import {View, StyleSheet} from 'react-native';

const CompensateTabBar = () => {
    return (
        <View style={styles.container}>
        </View>
    );
}

const styles = StyleSheet.create({
    container: {
        height: 60,
        backgroundColor: 'white'
    }
})

export default CompensateTabBar;
